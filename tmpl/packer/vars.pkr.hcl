proxmox_api_url = ${BOOTSTRAP_PROXMOX_API_URL}
proxmox_api_token_id = ${BOOTSTRAP_PROXMOX_API_TOKEN_ID}
proxmox_api_token_secret = ${BOOTSTRAP_PROXMOX_API_TOKEN_SECRET}

proxmox_build_os = ${BOOTSTRAP_PROXMOX_BUILD_OS}
proxmox_node = ${BOOTSTRAP_PROXMOX_NODE}
proxmox_vm_id = ${BOOTSTRAP_PROXMOX_VM_ID}
proxmox_vm_desc = ${BOOTSTRAP_PROXMOX_VM_DESC}
proxmox_iso_file = ${BOOTSTRAP_PROXMOX_ISO_FILE}
proxmox_iso_storage_pool = ${BOOTSTRAP_PROXMOX_ISO_STORAGE_POOL}
proxmox_cores = ${BOOTSTRAP_PROXMOX_CORES}
proxmox_memory = ${BOOTSTRAP_PROXMOX_MEMORY}
proxmox_http_bind_address = ${BOOTSTRAP_PROXMOX_HTTP_BIND_ADDRESS}
proxmox_ssh_username = ${BOOTSTRAP_PROXMOX_SSH_USERNAME}
proxmox_ssh_private_key = ${BOOTSTRAP_PROXMOX_SSH_PRIVATE_KEY_FILE}
