---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-settings
  namespace: flux-system
data:
  METALLB_LB_RANGE: ${BOOTSTRAP_KUBE_VIP_LB_RANGE}
  METALLB_TRAEFIK_ADDR: ${BOOTSTRAP_KUBE_VIP_TRAEFIK_ADDR}
