---
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: "${BOOTSTRAP_CLOUDFLARE_DOMAIN}"
  SECRET_CLOUDFLARE_EMAIL: "${BOOTSTRAP_CLOUDFLARE_EMAIL}"
  SECRET_ZIGBEE2MQTT_DEVICE: "${BOOTSTRAP_SVC_ZIGBEE2MQTT_DEVICE}"
  SECRET_VELERO_BUCKET_REGION: "${BOOTSTRAP_SVC_VELERO_BUCKET_REGION}"
  SECRET_VELERO_BUCKET_ENDPOINT: "${BOOTSTRAP_SVC_VELERO_BUCKET_ENDPOINT}"
  SECRET_VELERO_BUCKET_NAME: "${BOOTSTRAP_SVC_VELERO_BUCKET_NAME}"
  SECRET_LONGHORN_BACKUP_TARGET: "${BOOTSTRAP_SVC_LONGHORN_BACKUP_TARGET}"
  SECRET_CLUSTER_CD_TUNNEL_ID: "${BOOTSTRAP_CLUSTER_CD_TUNNEL_ID}"
  SECRET_CLUSTER_SELECTOR: "${BOOTSTRAP_CLUSTER_SELECTOR}"
