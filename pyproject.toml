[project]
name = "cluster-template"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
  "makejinja==2.8.2",
  "pydantic==2.13.4",
]

[dependency-groups]
dev = [
  "pytest>=8",
]

[tool.uv]
package = false
